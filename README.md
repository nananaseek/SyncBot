# Frontend packeges installing

- `yarn install` (before install yarn: `npm install -g yarn`)

# front development

- `yarn dev`

# front production

- `yarn build`

# To run backend

- setup `.env`

- `python manage.py runserver`
